/**
 * @deprecated Use getDefaultConnectors from @openfort/wagmi when building a wagmi config.
 * This export is kept for backwards compatibility but returns an empty object.
 */
export const wallets: Record<string, unknown> = {}
